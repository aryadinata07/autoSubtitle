# DeepSeek API Configuration
# Get your API key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_api_key_here

# Whisper Configuration
# 1 = Faster-Whisper (Recommended: 4-5x faster, same accuracy)
# 2 = Regular Whisper (Fallback option)
WHISPER_MODE=1

# Turbo Mode Configuration (Works with both Whisper implementations)
# true = Enable turbo mode (2-6x faster, greedy decoding, perfect for clear audio)
# false = Standard mode (beam search, maximum accuracy for noisy audio)
# ask = Ask user every time (default)
# 
# Turbo Mode Details:
# - Uses greedy decoding instead of beam search
# - Speed boost: Regular Whisper (2-3x), Faster-Whisper (3-6x)
# - 99% same accuracy for clear audio (YouTube, Podcast, TEDx)
# - Slightly less accurate for very noisy/challenging audio
# - Recommended for most use cases
TURBO_MODE=false

# Translation Method Configuration
# deepseek = Use DeepSeek AI (better quality, context-aware, requires API key)
# google = Use Google Translate (free, no API key required)
# ask = Ask user every time (default)
TRANSLATION_METHOD=deepseek

# Embedding Method Configuration
# soft = Soft Subtitle (INSTANT, 1-5 seconds, no re-encoding, recommended for YouTube/PC)
# fast = Hardsub - Fast Encoding (3-5 min for 17 min video, works on all platforms)
# gpu = Hardsub - GPU Accelerated (2-3 min for 17 min video, requires NVIDIA GPU)
# standard = Hardsub - Standard Quality (12-13 min for 17 min video, best quality but slow)
# ask = Ask user every time (default)
EMBEDDING_METHOD=ask

# Subtitle Styling Configuration
# Preset options: auto, minimal, standard, bold, reels, reels-bold
# auto = Auto-detect based on video orientation (vertical → reels, horizontal → minimal) [RECOMMENDED]
# minimal = Small font, thin outline, doesn't distract from video (for landscape)
# standard = Balanced, readable but not too dominant
# bold = Large font, thick outline, for better readability
# reels = Extra small font, optimized for vertical videos (Instagram Reels, TikTok, YouTube Shorts)
# reels-bold = Small font with better visibility for vertical videos
SUBTITLE_PRESET=auto

# Subtitle Position
# bottom = Bottom position (default)
# top = Top position
# center = Center position (not recommended)
SUBTITLE_POSITION=bottom

# Custom Styling (optional, override preset)
# Uncomment for custom styling
# SUBTITLE_FONT_SIZE=14
# SUBTITLE_OUTLINE=1
# SUBTITLE_MARGIN=10

# Subtitle Timing Configuration (Advanced)
# PENTING: Timing START dari Whisper sangat akurat, kita hanya extend END
# 
# Smart Timing Features:
# - Linguistic Bridge: Kalimat gantung (tanpa titik) akan di-hold sampai subtitle berikutnya
# - Reading Time: Subtitle duration disesuaikan dengan panjang text (15 char/second)
# - Sentence Detection: DeepSeek AI mendeteksi kalimat lengkap vs gantung
# 
SUBTITLE_MIN_DURATION=1.5  # Minimum subtitle duration (seconds) - increased for better readability
SUBTITLE_MAX_DURATION=8.0  # Maximum subtitle duration (seconds) - for long pauses
SUBTITLE_GAP=0.1  # Gap between subtitles (seconds) - smooth transitions

# Voice Activity Detection (VAD) Settings
# 1000ms = 1 detik, mencegah subtitle putus saat pembicara napas/jeda
# Jeda < 1 detik = masih satu kalimat, jangan potong!
VAD_MIN_SILENCE_MS=1000  # Minimum silence duration in milliseconds
